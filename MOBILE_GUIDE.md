# 🚇 지하철 폰 코딩 3단계 매뉴얼

> 폰과 회사 PC를 넘나들며 「미니 떡볶이 타이쿤 3D」를 끊김 없이 개발하는 방법입니다.
> 핵심 원리는 딱 하나예요: **"소스코드의 본체를 내 컴퓨터가 아니라 GitHub(클라우드 금고)에 두는 것."**
> 그러면 폰이든 PC든 어디서 열어도 항상 같은 최신 코드로 작업할 수 있습니다.

---

## 1단계. GitHub 금고 만들기 (PC에서 딱 1번만!)

로컬 커밋은 이미 완료되어 있어요. GitHub에 올리는 것만 하면 됩니다.

1. [github.com](https://github.com) 접속 → 로그인 (계정 없으면 무료 가입, 2분)
2. 우측 상단 **`+` → New repository** 클릭
   - Repository name: `tteokbokki-tycoon-3d` (원하는 이름 아무거나)
   - **Private** 선택 (나만 보기) → **Create repository** 버튼
3. 만들어진 저장소 주소를 복사 (예: `https://github.com/내아이디/tteokbokki-tycoon-3d.git`)
4. **클로드코드에게 이렇게 말하세요**:
   > "여기로 푸시해 줘: https://github.com/내아이디/tteokbokki-tycoon-3d.git"

   (직접 하고 싶다면 프로젝트 폴더 터미널에서:
   `git remote add origin 복사한주소` → `git push -u origin main`
   — 처음 한 번 브라우저 로그인 창이 뜨면 승인해 주면 끝!)

### 🎁 보너스: 폰에서 게임을 바로 보는 무료 링크 만들기
GitHub 저장소 페이지에서 **Settings → Pages → Branch를 `main`으로 선택 → Save**.
1분 뒤부터 `https://내아이디.github.io/tteokbokki-tycoon-3d/index3d.html` 주소로
**코드가 푸시될 때마다 자동 갱신되는 게임 링크**가 생깁니다. (Netlify 수동 업로드 졸업!)

---

## 2단계. 폰에서 작업 환경 열기 (둘 중 취향대로)

### 🅰️ 제일 간편: Claude 웹/앱에서 바로 (추천 ⭐)
1. 폰 브라우저에서 **claude.ai** 접속 (또는 Claude 앱) → **Code** 메뉴
2. **GitHub 저장소 연결** → 1단계에서 만든 저장소 선택
3. 끝! 지금 PC에서 하듯이 **"메뉴에 김밥 추가해 줘"** 라고 말로 시키면
   클라우드의 클로드코드가 코드를 고치고 커밋/푸시까지 해줍니다.
4. 결과 확인은 위에서 만든 **GitHub Pages 게임 링크**를 새로고침하면 끝.

### 🅱️ 터미널 + 3D 미리보기를 한 화면에: Firebase Studio (구 Project IDX)
1. 폰 브라우저에서 **studio.firebase.google.com** 접속 (구글 계정 로그인)
2. **Import Repo** → GitHub 저장소 주소 붙여넣기
3. 프로젝트에 들어있는 `.idx/dev.nix` 설정을 자동으로 읽어서
   ✅ 우측에 **3D 게임 미리보기 패널**, ✅ 하단에 **터미널**이 세팅됩니다
   (클로드코드 CLI도 자동 설치되게 해뒀어요)
4. 터미널에 `claude` 입력 → 처음 1회 로그인 → 지금과 똑같은 클로드코드 사용!

### 🅲️ 보너스: 3초 미리보기 전용 — StackBlitz
`https://stackblitz.com/github/내아이디/tteokbokki-tycoon-3d`
주소만 치면 설치 없이 코드+게임이 즉시 열립니다 (구경/간단 수정용).

---

## 3단계. 지하철 → 회사 PC 이어달리기 루틴

| 언제 | 하는 일 |
|---|---|
| 🚇 지하철에서 | 폰으로 2단계 환경 열기 → 클로드에게 작업 시키기 → 끝나면 **"커밋하고 푸시해 줘"** 한마디 |
| 🏢 회사 PC에서 | 프로젝트 폴더에서 클로드코드 켜고 **"git pull 해서 최신으로 받아줘"** → 이어서 작업 |
| 🏠 퇴근 후 | 어디서 작업했든 GitHub에는 항상 최신본 → 아무 기기에서나 이어서 |

> 💡 **딱 하나만 기억하세요**: 작업을 마칠 때마다 "커밋하고 푸시해 줘",
> 작업을 시작할 때마다 "풀(pull) 받아줘". 이 두 마디면 절대 코드가 꼬이지 않습니다.

---

## ❓ 자주 묻는 질문

- **폰에서 게임 조작 테스트도 되나요?** → 네! GitHub Pages 링크(1단계 보너스)를 폰 브라우저로 열면 터치/핀치줌/회전 전부 실기기로 테스트됩니다.
- **세이브는 공유되나요?** → 게임 세이브(localStorage)는 기기·주소별로 따로예요. 코드만 동기화됩니다.
- **뭔가 꼬인 것 같아요!** → 당황하지 말고 클로드코드에게 "git 상태 확인하고 정리해 줘"라고 하면 됩니다. GitHub에 올려둔 커밋은 사라지지 않아요.
